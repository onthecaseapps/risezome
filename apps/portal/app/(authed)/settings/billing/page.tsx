import type { ReactElement } from 'react';
import { SettingsStub } from '../_stub';

/** Settings → Billing (stub until the page is built). */
export default function BillingSettingsPage(): ReactElement {
  return <SettingsStub title="Billing" blurb="Plan, usage, and payment details." />;
}
