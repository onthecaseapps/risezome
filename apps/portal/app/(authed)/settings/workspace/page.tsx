import type { ReactElement } from 'react';
import { SettingsStub } from '../_stub';

/** Settings → Workspace (stub until the page is built). */
export default function WorkspaceSettingsPage(): ReactElement {
  return <SettingsStub title="Workspace" blurb="Workspace name, defaults, and configuration." />;
}
