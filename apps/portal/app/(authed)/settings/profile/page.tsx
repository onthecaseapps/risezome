import type { ReactElement } from 'react';
import { SettingsStub } from '../_stub';

/** Settings → Profile (stub until the page is built). */
export default function ProfileSettingsPage(): ReactElement {
  return <SettingsStub title="Profile" blurb="Your personal account details and preferences." />;
}
