-- Dev-only seed data. `supabase db reset` runs this after migrations.
-- Production deployments do NOT run seed.sql. Keep it minimal — just
-- enough to make `supabase start` produce a useful playground.

-- Example: one dev org with two stub members. Uncomment when you have a
-- consistent dev user UUID to bind against. Until then, the seed is
-- empty so a fresh local stack has the schema but no rows.

-- insert into public.orgs (id, name) values
--   ('00000000-0000-4000-8000-000000000001', 'Dev Org');
